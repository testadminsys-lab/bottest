pull_request_rules:
  - name: Automatic merge on approval
    conditions:
      - "#approved-reviews-by>=1"
    actions:
      merge:
        method: merge
pull_request_rules:
  - name: automatic merge for everyone
    conditions:
      - base=main
    actions:
      merge:
        method: merge